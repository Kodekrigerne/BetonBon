@using BetonBon.Client.RefitInterfaces
@using BetonBon.Client.Shared.Components
@using BetonBon.Shared.Models
@inject NavigationManager Navigation
@inject IEconomicApi _economicApi


<FullScreenModal IsVisible="ShowMaterialModal" ZIndex="120" SelectedAnimationStyle="FullScreenModal.ModalAnimationStyles.BottomToTop" Title="Ny Materialeregistrering" OnClose="OnClosed">

    @* Form Content *@
    <div class="content">
        
        @* Context Info *@
        <div class="context-box">
            <div>
                <div class="context-label">Projekt</div>
                <div class="context-value">@Project.Name</div>
            </div>
            <div class="divider"></div>
            <div class="activity-row" @onclick="ChangeActivity">
                <div>
                    <div class="context-label">Valgt Aktivitet (Tryk for at ændre)</div>
                    <div class="context-value-blue">@Activity.Name</div>
                </div>
                <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="#3b82f6" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M17 3a2.828 2.828 0 1 1 4 4L7.5 20.5 2 22l1.5-5.5L17 3z"></path></svg>
            </div>
        </div>

        @* Date Picker *@
        <div class="card">
            <div class="card-title-row">
                <label class="card-label">Vælg Dato</label>
                @if (IsToday)
                {
                    <span class="today-badge">I dag</span>
                }
            </div>
            <input type="date" @bind="RegistrationDate" class="date-input" />
        </div>

        @* Material Selection *@
        <div class="card" style="padding: 1.25rem;">
            <label class="card-label" style="display:block; margin-bottom: 1rem;">Hvilket materiale har du brugt?</label>
            
            @if (SelectedMaterial == null)
            {
                <button @onclick="@OpenPicker" class="material-empty-btn">
                    <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><line x1="12" y1="5" x2="12" y2="19"></line><line x1="5" y1="12" x2="19" y2="12"></line></svg>
                    Vælg et materiale
                </button>
            }
            else
            {
                <div class="selected-material-card">
                    <div>
                        <div class="smc-name">@SelectedMaterial.Name</div>
                        <div class="smc-unit">Måles i: @SelectedMaterial.Unit</div>
                    </div>
                    
                    <div class="smc-quantity-row">
                        <span class="smc-quantity-label">Mængde:</span>
                        <div class="quantity-controls">
                            <button @onclick="DecreaseQuantity" class="qty-btn qty-btn-minus">
                                <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><line x1="5" y1="12" x2="19" y2="12"></line></svg>
                            </button>
                            <span class="qty-value">@Quantity</span>
                            <button @onclick="IncreaseQuantity" class="qty-btn qty-btn-plus">
                                <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><line x1="12" y1="5" x2="12" y2="19"></line><line x1="5" y1="12" x2="19" y2="12"></line></svg>
                            </button>
                        </div>
                    </div>
                </div>
                
                <button @onclick="@OpenPicker" class="change-material-btn">
                    <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M17 3a2.828 2.828 0 1 1 4 4L7.5 20.5 2 22l1.5-5.5L17 3z"></path></svg>
                    Skift valgte materiale
                </button>
            }
        </div>

        @* Note Input *@
        <div class="card">
            <label class="card-label" style="display:block; margin-bottom:0.5rem;">Note (valgfri)</label>
            <textarea 
                @bind="Note" 
                placeholder="Skriv ekstra information her, f.eks. hvis der skete noget uventet..." 
                class="note-input"></textarea>
        </div>

    </div>

    @* Bottom Save Button *@
    <div class="bottom-bar">
        <button @onclick="SaveRegistration" class="save-btn">
            <svg xmlns="http://www.w3.org/2000/svg" width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"></path><polyline points="22 4 12 14.01 9 11.01"></polyline></svg>
            Gem registrering
        </button>
    </div>

</FullScreenModal>

<MaterialsPickPage IsVisible="_materialsPickerIsVisible" OnClosed="CloseMaterialPicker" MaterialSelected="SelectMaterial" SelectedMaterial="SelectedMaterial"/>