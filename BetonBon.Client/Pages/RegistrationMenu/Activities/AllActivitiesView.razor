@using BetonBon.Client.RefitInterfaces
@using BetonBon.Client.Services
@using BetonBon.Client.Shared.Components
@inject IEconomicRelayApi _economicApi
@inject PopupService _popUpService

<FullScreenModal IsVisible="IsVisible" Title="Vælg Aktivitet" OnClose="@Close" SelectedAnimationStyle="FullScreenModal.ModalAnimationStyles.RightToLeft" ZIndex="101">

        <SearchComponent @bind-SearchTerm="@Search" @bind-SearchTerm:after="OnSearch" />

        <div class="list-area">
         
            @foreach (var activity in _filteredActivities)
            {
               <ActivityCard ActivitySelected="SelectActivity" SelectedActivity="activity"/>
            }
    </div>

</FullScreenModal>
